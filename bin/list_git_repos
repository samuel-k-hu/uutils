#!/usr/bin/perl

use strict;
use File::Find;
use Cwd;

my $starting_point = @ARGV > 0 ? $ARGV[0] : getcwd();

find (
    sub { 
        print $File::Find::dir, "\n" if -d $_ && $_ eq '.git'; 
        },
    $starting_point
)
